kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: python-netcat-app
spec:
  to:
    kind: Service
    name: python-netcat-app
    weight: 100
  port:
    targetPort: 80
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None